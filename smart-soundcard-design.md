# 智能声卡系统设计方案

## S3 ↔ Type-C ↔ 树莓派 · 语音交互前端

---

## 1. 系统概述

### 1.1 设计目标

将立创·实战派 ESP32-S3 开发板打造为一个高性能"智能声卡"，通过 Type-C USB 连接树莓派，形成分工明确的语音交互系统：S3 承担高频、实时的音频信号处理（降噪、回声消除、唤醒词检测），树莓派承担高延迟、高算力的云端交互（ASR 语音识别、LLM 推理、TTS 语音合成）。

### 1.2 整体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                         立创·实战派 S3                             │
│                                                                  │
│   ┌─────────┐    ┌──────────────┐    ┌──────────────┐           │
│   │ ES7210  │───▶│  AFE 引擎     │───▶│  WakeNet     │           │
│   │ 双麦克风 │    │ 语音前端处理  │    │  唤醒词检测   │           │
│   └─────────┘    └──────┬───────┘    └──────┬───────┘           │
│                         │                    │                    │
│   ┌─────────┐    ┌──────▼───────┐    ┌──────▼───────┐           │
│   │ ES8311  │◀───│  音频输出     │    │  LCD UI      │           │
│   │ 扬声器   │    │ (TTS 播放)   │    │  状态显示     │           │
│   └─────────┘    └──────────────┘    └──────────────┘           │
│                         │                    │                    │
│                  ┌──────▼────────────────────▼───────┐           │
│                  │      USB 复合设备 (TinyUSB)        │           │
│                  │  ┌──────────┐  ┌───────────────┐  │           │
│                  │  │ USB-UAC  │  │   USB-CDC     │  │           │
│                  │  │ 音频流   │  │   控制指令     │  │           │
│                  │  └──────────┘  └───────────────┘  │           │
│                  └──────────────┬────────────────────┘           │
└─────────────────────────────────┼────────────────────────────────┘
                                  │ Type-C USB
┌─────────────────────────────────┼────────────────────────────────┐
│                         树莓派  │                                 │
│                  ┌──────────────▼────────────────────┐           │
│                  │      USB Host (snd-usb-audio)     │           │
│                  │  ┌──────────┐  ┌───────────────┐  │           │
│                  │  │ ALSA     │  │  /dev/ttyACMx │  │           │
│                  │  │ 音频捕获  │  │  串口控制      │  │           │
│                  │  └────┬─────┘  └───────┬───────┘  │           │
│                  └───────┼────────────────┼──────────┘           │
│                          │                │                      │
│                  ┌───────▼────┐    ┌──────▼───────┐              │
│                  │ 语音管线    │    │  状态管理器   │              │
│                  │ ASR → LLM  │    │  指令路由     │              │
│                  │ → TTS      │    │              │              │
│                  └────────────┘    └──────────────┘              │
│                                                                  │
│                  ┌────────────────────────────────┐              │
│                  │        云端 API 网关            │              │
│                  │  ASR / LLM / TTS / IoT 控制    │              │
│                  └────────────────────────────────┘              │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. 物理层与协议设计

### 2.1 USB 复合设备架构

通过 Type-C 连接时，S3 配置为 USB 复合设备（Composite Device），同时暴露两个 USB 接口：

| 接口 | USB Class | 用途 | 数据特征 |
|------|-----------|------|----------|
| 接口 0 | USB-UAC 2.0 (Audio Class) | 音频流传输 | 等时传输，实时性保证 |
| 接口 1 | USB-CDC (Virtual Serial Port) | 控制指令 | 批量传输，可靠性保证 |

USB-UAC 将 S3 模拟为一个标准的"USB 麦克风 + USB 扬声器"，树莓派端无需安装特殊驱动，Linux 内核的 `snd-usb-audio` 模块自动识别。USB-CDC 作为虚拟串口，传输 JSON 格式的控制指令。

### 2.2 USB-UAC 音频参数

| 参数 | 麦克风方向 (S3 → Pi) | 扬声器方向 (Pi → S3) |
|------|----------------------|----------------------|
| 采样率 | 16,000 Hz | 16,000 Hz |
| 位深度 | 16-bit (S16_LE) | 16-bit (S16_LE) |
| 声道数 | 1 (Mono) | 1 (Mono) |
| 数据率 | 32 KB/s (256 kbps) | 32 KB/s (256 kbps) |

选择 16 kHz / 16-bit / Mono 的理由：这是语音识别（ASR）的标准输入格式，也是 ESP-SR AFE 管线的原生输出格式。总音频带宽仅 512 kbps，远低于 USB Full-Speed 的 12 Mbps 上限，为 CDC 串口留有充足余量。

### 2.3 USB-CDC 控制协议

CDC 接口以 JSON 行（JSON Lines）格式传输控制消息，每条消息以 `\n` 结尾。波特率设置为 115200（虚拟串口实际速率由 USB 带宽决定，该参数仅为兼容性）。

**S3 → 树莓派方向（事件上报）：**

```json
{"event": "wakeup", "ts": 1234567890}
{"event": "vad_start", "ts": 1234567891}
{"event": "vad_end", "ts": 1234567895, "duration_ms": 4000}
{"event": "playback_done", "ts": 1234567900}
{"event": "heartbeat", "ts": 1234567903}
{"event": "status", "state": "idle", "cpu": 45, "mem_free": 102400}
```

**树莓派 → S3 方向（控制指令）：**

```json
{"cmd": "set_state", "state": "listening"}
{"cmd": "set_state", "state": "thinking"}
{"cmd": "set_state", "state": "speaking"}
{"cmd": "set_state", "state": "idle"}
{"cmd": "set_volume", "level": 75}
{"cmd": "play_tone", "tone": "wakeup_ack"}
{"cmd": "set_wakeword", "enabled": true}
```

### 2.4 实现依赖

USB 复合设备的实现推荐使用 Espressif 官方的 `usb_device_uac` 组件（v1.2.3，ESP Component Registry，使用前请[验证最新版本](https://components.espressif.com/components/espressif/usb_device_uac)），而非直接操作 TinyUSB 底层 API。CDC 部分使用 ESP-IDF 内置的 `esp_tinyusb` CDC 驱动。两者需要通过自定义 USB 描述符组合为复合设备。

**已知限制与应对策略：**

| 限制 | 影响 | 应对 |
|------|------|------|
| `usb_device_uac` 不能同时兼容 Windows 和 Linux 描述符 | 树莓派专用场景无影响 | 仅配置 Linux 兼容描述符 |
| 不支持运行时动态切换采样率 | 固定 16 kHz 即可 | 编译时通过 menuconfig 配置 |
| macOS 需要 `UAC_SUPPORT_MACOS` 宏 | 开发调试时需注意 | 仅在需要 macOS 调试时启用 |
| ESP32-S3 仅支持 USB Full-Speed (12 Mbps) | 语音场景带宽充足 | 无需特殊处理 |

---

## 3. S3 端：语音前端处理架构

### 3.1 硬件资源映射

立创·实战派 ESP32-S3 板载音频硬件：

| 芯片 | 功能 | 接口 | 关键引脚 |
|------|------|------|----------|
| ES7210 | 4 通道 ADC（麦克风输入） | I2S TDM + I2C | I2S: GPIO12(DIN), GPIO13(WS), GPIO14(BCLK), GPIO38(MCLK)；I2C: GPIO1(SDA), GPIO2(SCL), Addr=0x41 |
| ES8311 | DAC + 功放（扬声器输出） | I2S + I2C | 共享 I2S 总线 |
| LCD | 状态显示屏 | SPI | — |

**ES7210 通道分配：**

| 通道 | 连接 | 用途 |
|------|------|------|
| MIC1 | 板载麦克风 1 | 语音采集（双麦阵列通道 A） |
| MIC2 | 板载麦克风 2 | 语音采集（双麦阵列通道 B） |
| MIC3 | ES8311 输出回环 | AEC 回声消除参考信号 |
| MIC4 | 未连接 | — |

这一硬件设计使得 AEC 参考信号通过硬件回环获取，无需软件层面的额外布线。

### 3.2 AFE 音频前端引擎 (ESP-SR)

AFE（Audio Front-End）是 ESP-SR 框架的核心模块，负责将原始麦克风信号处理为干净的语音流。处理管线如下：

```
ES7210 I2S TDM (48 kHz, 3ch)
        │
        ▼
  ┌─────────────┐
  │  降采样 3:1  │   48 kHz → 16 kHz
  └──────┬──────┘
         ▼
  ┌─────────────┐
  │    AEC      │   回声消除（MIC3 参考信号）
  │  FD_LOW_COST│   全双工低成本模式
  └──────┬──────┘
         ▼
  ┌─────────────┐
  │    BSS      │   盲源分离（双麦波束成形）
  └──────┬──────┘
         ▼
  ┌─────────────┐
  │    NS       │   单通道背景降噪
  └──────┬──────┘
         ▼
  ┌─────────────┐
  │    VAD      │   语音活动检测
  └──────┬──────┘
         ▼
  ┌─────────────┐
  │    AGC      │   自动增益控制
  └──────┬──────┘
         ▼
  干净语音流 (16 kHz, 16-bit, Mono)
    ├──▶ USB-UAC 输出
    └──▶ WakeNet 检测
```

**AFE 配置要点：**

- 输入格式：16-bit 有符号 PCM，通道交错排列，通道映射 `"MMR"`（MIC1, MIC2, REF）
- AEC 模式选择 `AEC_MODE_FD_LOW_COST`（全双工低成本），适合板载同体扬声器/麦克风场景
- AEC 滤波器长度建议设为 4
- 所有缓冲区需 16 字节对齐
- VAD 灵敏度需根据实际环境噪声水平调优

**资源占用估算：**

| 模块 | Internal SRAM | PSRAM | CPU (单核 @240MHz) |
|------|---------------|-------|-------------------|
| AFE (2-MIC + AEC) | ~50-80 KB | ~100-200 KB | ~20-30% |
| WakeNet9 | ~20-30 KB | ~100-200 KB | ~10-15% |
| USB (UAC + CDC) | ~10-15 KB | — | ~5% |
| LCD UI | ~5 KB | ~20 KB (帧缓冲) | ~3% |
| **合计** | **~85-130 KB** | **~220-420 KB** | **~38-53%** |

推荐使用 ESP32-S3-WROOM-1-N16R8 模组（16 MB Flash, 8 MB PSRAM），以确保充足的资源余量。

### 3.3 离线唤醒词引擎 (WakeNet)

使用 ESP-SR 的 WakeNet9 模型实现离线唤醒词检测。

**工作参数：**

- 输入：AFE 输出的 16 kHz / 16-bit / Mono 干净语音
- 帧窗口：30 ms
- 特征提取：内置 MFCC
- 单模型最多支持 5 个唤醒词

**预置唤醒词：** "Hi Lexin"、"你好小鑫" 等。如需自定义唤醒词（如"小爪"），有两条路径：

| 方案 | 说明 | 成本 | 适用场景 |
|------|------|------|----------|
| TTS 合成训练 (V3) | 使用语音合成样本训练，无需真人录音 | 低（自行完成） | 原型验证、小批量 |
| 专业定制服务 | 乐鑫官方训练，需 500+ 说话人样本 | 高（数千美元） | 量产产品 |

**唤醒流程：**

```
AFE 输出 ──▶ WakeNet 检测
                 │
            检测到唤醒词
                 │
                 ▼
        ┌────────────────┐
        │ 1. LED/LCD 反馈 │  立即显示"听取中"
        │ 2. 播放提示音   │  蜂鸣 / "嗯？"
        │ 3. CDC 上报事件  │  {"event":"wakeup"}
        │ 4. 开始录音转发   │  AFE → USB-UAC
        └────────────────┘
```

### 3.4 LCD UI 状态机

LCD 通过 SPI 驱动，采用状态机管理 UI 显示：

```
          ┌───────────────────────────────────────┐
          │                                       │
          ▼                                       │
    ┌──────────┐   唤醒词触发   ┌──────────────┐  │
    │   IDLE   │──────────────▶│  LISTENING   │  │
    │  待机状态 │               │   听取中      │  │
    │ (动态眼睛) │               │ (声波波形)    │  │
    └──────────┘               └──────┬───────┘  │
          ▲                           │          │
          │                      VAD 结束        │
          │                           │          │
          │                    ┌──────▼───────┐  │
          │                    │  THINKING    │  │
          │                    │   思考中      │  │
          │  完成/超时          │ (加载动画)    │  │
          │                    └──────┬───────┘  │
          │                           │          │
          │                    LLM 响应到达       │
          │                           │          │
          │                    ┌──────▼───────┐  │
          │                    │  SPEAKING    │  │
          └────────────────────│   播报中      │──┘
               TTS 播放结束     │ (嘴巴动画)    │
                               └──────────────┘
```

**各状态渲染方式：**

| 状态 | 显示内容 | 动画帧率 | 触发条件 |
|------|---------|---------|---------|
| IDLE | 卡通眼睛，随机眨眼 | 10 FPS | 默认 / TTS 播放结束 / 超时 |
| LISTENING | 声波波形，跟随 VAD 振幅 | 20 FPS | 唤醒词检测成功 |
| THINKING | 旋转加载圆环 | 15 FPS | VAD 检测到语音结束 |
| SPEAKING | 嘴巴张合动画，跟随 TTS 振幅 | 15 FPS | TTS 音频开始播放 |

状态切换由 CDC 指令或本地事件驱动。为避免 LCD 刷新影响音频实时性，UI 渲染应放在独立的低优先级 FreeRTOS 任务中。

### 3.5 固件任务架构 (FreeRTOS)

S3 固件采用多任务架构，任务间通过 FreeRTOS Queue 和 Event Group 通信：

```
┌──────────────────────────────────────────────────────┐
│                    FreeRTOS 任务分布                    │
│                                                      │
│  Core 0:                                             │
│  ┌──────────────────┐  ┌──────────────────┐          │
│  │  AFE Feed Task   │  │  AFE Fetch Task  │          │
│  │  优先级: 最高 (23) │  │  优先级: 高 (22)  │          │
│  │  栈: 4 KB        │  │  栈: 4 KB        │          │
│  │  职责: I2S → AFE  │  │  职责: AFE → 输出  │          │
│  └──────────────────┘  └──────────────────┘          │
│                                                      │
│  Core 1:                                             │
│  ┌──────────────────┐  ┌──────────────────┐          │
│  │  USB Task        │  │  CDC Task        │          │
│  │  优先级: 高 (20)  │  │  优先级: 中 (15)  │          │
│  │  栈: 4 KB        │  │  栈: 2 KB        │          │
│  │  职责: UAC 音频收发│  │  职责: 指令解析    │          │
│  └──────────────────┘  └──────────────────┘          │
│  ┌──────────────────┐  ┌──────────────────┐          │
│  │  LCD Task        │  │  Audio Out Task  │          │
│  │  优先级: 低 (5)   │  │  优先级: 高 (19)  │          │
│  │  栈: 3 KB        │  │  栈: 3 KB        │          │
│  │  职责: UI 渲染    │  │  职责: TTS 回放   │          │
│  └──────────────────┘  └──────────────────┘          │
└──────────────────────────────────────────────────────┘
```

**任务间数据流：**

- AFE Fetch → USB Task：环形缓冲区（Ring Buffer），容量 4096 帧（256 ms @16 kHz）
- USB Task → Audio Out：环形缓冲区，容量 8192 帧（512 ms），用于 TTS 播放缓冲
- CDC Task → 各任务：FreeRTOS Event Group，按位通知状态变更
- AFE Fetch → LCD Task：FreeRTOS Queue，传递 VAD 状态和振幅值

---

## 4. 树莓派端：云端交互架构

### 4.1 软件栈概览

树莓派运行 Raspberry Pi OS (Bookworm)，使用 Python 3.11+ 作为主要开发语言：

```
┌──────────────────────────────────────────────┐
│                应用层                         │
│  ┌────────────────────────────────────────┐  │
│  │        主控状态机 (Main Controller)      │  │
│  │  负责协调所有模块的工作流程               │  │
│  └──────────────────┬─────────────────────┘  │
│                     │                         │
│  ┌──────┬───────────┼───────────┬──────────┐ │
│  │      │           │           │          │ │
│  ▼      ▼           ▼           ▼          │ │
│ ┌────┐ ┌─────┐  ┌──────┐  ┌─────────┐     │ │
│ │CDC │ │Audio│  │ ASR  │  │   LLM   │     │ │
│ │串口 │ │音频 │  │ 模块 │  │  模块   │     │ │
│ │管理 │ │捕获 │  │      │  │         │     │ │
│ └────┘ └─────┘  └──────┘  └─────────┘     │ │
│                                    │        │ │
│                              ┌─────▼─────┐  │ │
│                              │   TTS     │  │ │
│                              │   模块    │  │ │
│                              └───────────┘  │ │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │          ALSA / PortAudio               │  │
│  │      snd-usb-audio 内核驱动             │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### 4.2 音频捕获模块

树莓派通过 ALSA 从 USB-UAC 设备捕获音频，推荐使用 `sounddevice` 库：

**设备枚举与选择：**

USB-UAC 设备插入后，Linux 内核自动加载 `snd-usb-audio` 驱动，设备出现在 `arecord -l` 列表中。应用层通过设备名称匹配（如包含 "ESP32" 或自定义产品名）自动选择正确的音频设备，避免硬编码 card/device 编号。

**ALSA 参数调优：**

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| period_size | 512 帧 | 512 / 16000 = 32 ms，平衡延迟与稳定性 |
| periods | 4 | 缓冲区 = 4 × 32 ms = 128 ms |
| format | S16_LE | 与 S3 端一致 |
| rate | 16000 | 与 S3 端一致 |
| channels | 1 | Mono |

**捕获延迟估算：** USB Full-Speed 等时传输帧间隔 1 ms + ALSA 缓冲 32 ms ≈ 33 ms 端到端捕获延迟。对于语音识别场景完全可接受。

### 4.3 CDC 串口管理

通过 `pyserial` 监听 `/dev/ttyACMx` 设备，解析 S3 上报的 JSON 事件：

**设备发现策略：** 使用 `pyserial.tools.list_ports` 按 VID/PID 或产品字符串自动发现 S3 的 CDC 端口，支持热插拔检测。

**消息处理：** 使用独立线程持续读取串口数据，按行分割后 JSON 解析，通过 `asyncio.Queue` 分发到主控状态机。

### 4.4 语音管线 (ASR → LLM → TTS)

主控状态机协调完整的语音交互流程：

```
收到 "wakeup" 事件
        │
        ▼
  发送 {"cmd":"set_state","state":"listening"}
        │
        ▼
  开始录音（从 ALSA 读取 USB-UAC 音频）
        │
  收到 "vad_end" 事件
        │
        ▼
  发送 {"cmd":"set_state","state":"thinking"}
  停止录音，将音频发送至 ASR
        │
        ▼
  ┌─────────────────┐
  │   ASR 云端识别    │   推荐: Whisper API / 阿里云 ASR
  │   或流式识别      │   备选: 本地 whisper.cpp (Pi 5)
  └────────┬────────┘
           │ 文本结果
           ▼
  ┌─────────────────┐
  │   LLM 推理       │   推荐: OpenAI / 通义千问 API
  │   流式输出        │   备选: 本地 Ollama (Pi 5, 8GB)
  └────────┬────────┘
           │ 流式文本
           ▼
  ┌─────────────────┐
  │   TTS 合成       │   推荐: Edge-TTS (免费) / 阿里云 TTS
  │   流式播放        │   备选: Piper TTS (本地离线)
  └────────┬────────┘
           │ 音频流
           ▼
  发送 {"cmd":"set_state","state":"speaking"}
  将 TTS 音频通过 USB-UAC 扬声器通道发送至 S3
        │
  音频播放结束
        │
        ▼
  发送 {"cmd":"set_state","state":"idle"}
```

### 4.5 ASR 方案选择

| 方案 | 延迟 | 准确率 | 成本 | 适用场景 |
|------|------|--------|------|----------|
| OpenAI Whisper API | ~1-3s | 极高 | 按分钟计费 | 通用场景首选 |
| 阿里云 ASR 流式版 | ~300ms | 高（中文优秀） | 按次计费 | 中文场景优选 |
| whisper.cpp (本地) | ~3-8s (Pi 5) | 高 | 免费 | 离线/隐私敏感场景 |
| Sherpa-ONNX (本地) | ~1-2s (Pi 5) | 中高 | 免费 | 低延迟离线场景 |

**流式 ASR 优化：** 如果选择支持流式识别的 ASR 服务（如阿里云），可以在用户说话的同时就开始识别，将 ASR 延迟从"说完后等待"缩短为"说完即出结果"。实现方式为：在 `vad_start` 事件后立即开始流式推送音频分片。

### 4.6 TTS 回传与回声消除联动

TTS 合成的音频需要通过 USB-UAC 的扬声器通道发送回 S3 播放。关键要点：

1. TTS 输出需重采样为 16 kHz / 16-bit / Mono（与 UAC 配置一致）
2. S3 的 ES8311 播放此音频时，MIC3 同时采集回环信号
3. AFE 的 AEC 模块利用 MIC3 信号消除回声，实现"边说边听"能力
4. 若不需要 barge-in（打断），可以在 SPEAKING 状态下临时禁用唤醒检测以节省 CPU

---

## 5. 端到端数据流与时序分析

### 5.1 完整交互时序图

```
  用户           S3 (ESP32-S3)              树莓派               云端
   │                  │                       │                   │
   │   "小爪"         │                       │                   │
   │ ─────────────▶   │                       │                   │
   │              WakeNet 检测 (~200ms)        │                   │
   │                  │                       │                   │
   │                  │──CDC: {"wakeup"}─────▶│                   │
   │                  │                       │                   │
   │                  │◀─CDC: set_state───────│                   │
   │                  │  "listening"          │                   │
   │                  │                       │                   │
   │   "今天天气怎样"  │                       │                   │
   │ ─────────────▶   │                       │                   │
   │              AFE 处理 (~10ms)             │                   │
   │                  │──UAC: 音频流──────────▶│                   │
   │                  │                       │──ASR 流式推送─────▶│
   │                  │                       │                   │
   │   (停顿)         │                       │                   │
   │                  │ VAD 检测到语音结束      │                   │
   │                  │──CDC: {"vad_end"}────▶│                   │
   │                  │                       │                   │
   │                  │◀─CDC: set_state───────│                   │
   │                  │  "thinking"           │                   │
   │                  │                       │◀─ASR 结果──────────│
   │                  │                       │──LLM 请求─────────▶│
   │                  │                       │◀─LLM 流式响应──────│
   │                  │                       │──TTS 合成─────────▶│
   │                  │                       │◀─TTS 音频──────────│
   │                  │                       │                   │
   │                  │◀─CDC: set_state───────│                   │
   │                  │  "speaking"           │                   │
   │                  │◀─UAC: TTS 音频────────│                   │
   │   "今天晴天..."   │                       │                   │
   │ ◀────────────    │ ES8311 播放             │                   │
   │                  │ AEC 同步消除回声        │                   │
   │                  │                       │                   │
   │                  │──CDC: set_state───────▶│                   │
   │                  │  播放结束 → "idle"      │                   │
   │                  │                       │                   │
```

### 5.2 延迟预算分析

| 阶段 | 耗时 | 累计 | 说明 |
|------|------|------|------|
| 唤醒词检测 | ~200 ms | 200 ms | WakeNet 处理延迟 |
| 唤醒反馈 | ~50 ms | 250 ms | CDC 传输 + 提示音播放 |
| 用户说话 | ~2-5 s | — | 用户可变 |
| VAD 尾部检测 | ~300-500 ms | 500 ms | VAD 需确认静音持续 |
| USB 音频传输 | ~33 ms | 533 ms | ALSA 缓冲延迟 |
| ASR 识别 | ~300-3000 ms | 833-3533 ms | 取决于方案（流式 vs 非流式） |
| LLM 推理 (首 token) | ~200-800 ms | 1033-4333 ms | 取决于模型和网络 |
| TTS 合成 (首段) | ~200-500 ms | 1233-4833 ms | 流式 TTS 可提前开始播放 |
| TTS 回传播放 | ~33 ms | 1266-4866 ms | USB + DAC 延迟 |

**关键优化策略：**

1. **流式 Pipeline：** ASR 流式输入 → LLM 流式输出 → TTS 流式合成 → 流式播放，各环节重叠执行，端到端感知延迟可控制在 1.5-2 秒
2. **首句优先：** LLM 输出第一句话后立即开始 TTS，不等全部生成完毕
3. **预缓冲：** TTS 音频预缓冲 200 ms 后开始播放，避免卡顿

---

## 6. 错误处理与恢复策略

### 6.1 USB 连接异常

| 场景 | 检测方式 | 恢复策略 |
|------|---------|---------|
| USB 断开 | S3: USB 事件回调；Pi: 设备文件消失 | S3 回退到离线模式（仅唤醒+提示"未连接"）；Pi 启动重连轮询 |
| USB 枚举失败 | S3: TinyUSB 枚举超时 | S3 重新初始化 USB 栈，最多重试 3 次后报错 |
| UAC 数据中断 | Pi: ALSA xrun 回调 | 自动重新打开音频流，丢弃断点前的不完整帧 |
| CDC 通信超时 | Pi: 心跳超时 (>5s) | Pi 尝试重新打开串口；若失败则 USB reset |

**心跳机制：** S3 每 3 秒通过 CDC 发送心跳 `{"event":"heartbeat","ts":...}`。树莓派若连续 2 次未收到心跳（>6 秒），判定连接异常并触发重连流程。

### 6.2 音频异常

| 场景 | 检测方式 | 恢复策略 |
|------|---------|---------|
| 音频缓冲区溢出 | Ring Buffer 写指针追上读指针 | 丢弃最旧数据，保留最新帧，日志告警 |
| 音频欠采样 (underrun) | 读指针追上写指针 | 插入静音帧，避免音频流中断 |
| 持续噪声 / VAD 误触发 | VAD 持续 >30s 无有效内容 | 自动取消录音，回到 IDLE，LCD 提示 |
| 唤醒词误检 | — | 设置唤醒后 10 秒无有效 VAD → 自动超时回到 IDLE |

### 6.3 云端服务异常

| 场景 | 恢复策略 |
|------|---------|
| ASR 请求超时 (>10s) | 回退到本地 ASR (whisper.cpp)；若无本地模型则 LCD 显示"网络异常" |
| LLM 请求超时 (>15s) | 超时后 TTS 播报"思考超时，请重试"并回到 IDLE |
| TTS 合成失败 | 使用本地 Piper TTS 兜底，或直接将文本通过 LCD 显示 |
| API Key 失效 | 启动时校验，失败后 LCD 显示配置引导二维码 |

### 6.4 看门狗与自恢复

S3 端启用 FreeRTOS Task Watchdog (TWDT)，监控关键任务：

- AFE Feed/Fetch Task：超时 5 秒触发重启
- USB Task：超时 10 秒触发 USB 栈重初始化
- 全局硬件看门狗：超时 30 秒触发芯片复位

树莓派端使用 `systemd` 管理主服务，配置 `Restart=always` 和 `WatchdogSec=60`。

---

## 7. 性能约束与硬件选型

### 7.1 S3 资源约束

| 资源 | 总量 (N16R8) | 预估占用 | 余量 |
|------|-------------|---------|------|
| Internal SRAM | 512 KB | ~130 KB | ~382 KB (74%) |
| PSRAM | 8 MB | ~420 KB | ~7.6 MB (95%) |
| Flash | 16 MB | ~4 MB (固件+模型) | ~12 MB (75%) |
| CPU Core 0 | 240 MHz | ~50% (AFE) | ~50% |
| CPU Core 1 | 240 MHz | ~30% (USB+LCD+Audio) | ~70% |

### 7.2 树莓派选型建议

| 型号 | CPU | RAM | 适用场景 |
|------|-----|-----|---------|
| Pi Zero 2 W | 4×1 GHz | 512 MB | 纯云端方案（无本地模型），成本敏感 |
| Pi 4B | 4×1.8 GHz | 2/4/8 GB | 推荐方案，平衡性能与成本 |
| Pi 5 | 4×2.4 GHz | 4/8 GB | 需要本地 ASR/TTS 时的最佳选择 |

### 7.3 功耗估算

| 组件 | 典型功耗 | 峰值功耗 |
|------|---------|---------|
| ESP32-S3 (WiFi 关闭, USB 活跃) | ~150 mW | ~250 mW |
| ES7210 + ES8311 | ~50 mW | ~80 mW |
| LCD 背光 | ~100 mW | ~150 mW |
| 树莓派 4B | ~3 W | ~6 W |
| **合计** | **~3.3 W** | **~6.5 W** |

S3 可由树莓派通过 Type-C 供电（USB 5V/500mA 即可满足），无需额外电源。

---

## 8. 技术评审与改进建议

### 8.1 方案优势

本方案的核心架构思路——将实时音频处理卸载到 S3、将高算力任务交给树莓派——是合理且成熟的设计模式。具体优势包括：S3 的 ESP-SR 框架在嵌入式语音前端领域有良好的生态和社区支持；USB-UAC 方案让树莓派端零驱动集成；双麦 + 硬件回环 AEC 参考信号的硬件设计相当精巧，避免了软件回环的同步问题。

### 8.2 风险与建议

**风险 1：USB-UAC 复合设备的工程复杂度**

`usb_device_uac` 组件（v1.2.3）虽然功能可用，但与 `esp_tinyusb` 的 CDC 驱动并非开箱即用的集成。需要手写复合设备描述符来同时暴露 UAC 和 CDC 接口。社区反馈该组件存在一些未修复的问题（如基础示例在某些 S3 板上无法正常工作）。

**建议：** 开发初期先分别验证 UAC-only 和 CDC-only 的功能，确认各自稳定后再合并为复合设备。准备一个降级方案：若 UAC 集成困难，可暂时改用 CDC 串口传输 PCM 数据（带宽足够，但需自行实现帧同步协议）。

**风险 2：48 kHz → 16 kHz 降采样**

ES7210 硬件运行在 48 kHz，而 ESP-SR 要求 16 kHz 输入。需要在软件层实现 3:1 降采样（decimation）。不当的降采样会引入混叠噪声。

**建议：** 使用抗混叠低通滤波器（截止频率 8 kHz）配合 3:1 抽取，或直接使用 ESP-SR AFE 内置的采样率转换功能（如果支持）。需在实际硬件上验证音质。

**风险 3：Barge-in（打断）场景的 AEC 稳定性**

当 TTS 正在播放时用户打断说话，AEC 需要同时消除回声并提取用户语音。在小尺寸 PCB 上，扬声器和麦克风物理距离很近，回声路径复杂，AEC 可能收敛不完全。

**建议：** 先以"非打断模式"上线（SPEAKING 状态禁用唤醒检测），后续通过实测调优 AEC 的 NLP 激进程度（`AEC_NLP_LEVEL_AGGR`）再开启 barge-in。如果 AEC 效果不理想，可以考虑添加一个物理静音按钮作为补充。

**风险 4：自定义唤醒词的可行性**

自定义唤醒词（如"小爪"）需要乐鑫官方的定制训练。TTS 合成训练 (V3) 方案虽然成本低，但效果（尤其是噪声环境下的召回率和误触发率）可能不及专业训练。两个字的唤醒词（"小爪"）相比三到四个字的（如"你好小爪"）更容易误触发。

**建议：** 将唤醒词改为三到四个音节（如"你好小爪"或"嘿小爪子"）以降低误触发率。开发初期先使用预置唤醒词（如"Hi Lexin"）验证完整链路，待系统稳定后再投入唤醒词定制。

**风险 5：UAC2 在树莓派上的兼容性**

社区有报告指出 UAC2 设备在树莓派上偶现 dropout（音频丢包），尤其在 USB 总线负载较高时。

**建议：** 考虑优先实现 UAC1（兼容性更广），仅在需要特定 UAC2 特性（如反馈端点）时才升级。或者在树莓派端配置更大的 ALSA 缓冲（4 periods × 512 frames）来吸收偶发抖动。

### 8.3 扩展方向

1. **WiFi 直连模式：** S3 自带 WiFi，可以在 USB 断开时回退为 WiFi 音频流（WebSocket + PCM），实现无线工作模式。
2. **多轮对话上下文：** 树莓派维护对话历史，支持连续追问而无需重复唤醒。
3. **IoT 控制集成：** LLM 输出结构化指令（如 `{"action":"turn_on","device":"light"}`），树莓派通过 MQTT/HTTP 控制智能家居设备。
4. **OTA 固件更新：** 通过 USB-CDC 或 WiFi 实现 S3 固件的远程更新。

---

## 附录 A：依赖清单

### S3 端 (ESP-IDF v5.2+)

| 组件 | 版本 | 来源 |
|------|------|------|
| esp-sr | latest | ESP Component Registry |
| usb_device_uac | v1.2.3 | ESP Component Registry |
| esp_tinyusb (CDC) | 内置 | ESP-IDF |
| es7210 | v1.0.1 | ESP Component Registry |
| es8311 | 内置 | ESP-IDF examples |
| lvgl / 自定义 LCD 驱动 | — | 视 LCD 型号选择 |

### 树莓派端 (Python 3.11+)

| 库 | 用途 |
|----|------|
| sounddevice | USB-UAC 音频捕获与播放 |
| pyserial | USB-CDC 串口通信 |
| numpy | 音频数据处理 |
| openai / dashscope | ASR / LLM API 调用 |
| edge-tts / piper-tts | TTS 语音合成 |
| asyncio | 异步任务调度 |

---

## 附录 B：关键参考资料

- [ESP-SR 官方文档 (AFE / WakeNet / AEC)](https://docs.espressif.com/projects/esp-sr/en/latest/esp32s3/)
- [usb_device_uac 组件](https://components.espressif.com/components/espressif/usb_device_uac)
- [TinyUSB CDC+UAC2 复合设备示例](https://github.com/hathach/tinyusb)
- [立创实战派 ESP32-S3 硬件文档](https://wiki.lckfb.com/zh-hans/szpi-esp32s3/)
- [Raspberry Pi USB Audio (snd-usb-audio)](https://wiki.linuxaudio.org/wiki/raspberrypi)
